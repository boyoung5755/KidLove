<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.KidLove.mber.dao.MberDAO">

	<select id="getMemberPushToken" resultType="com.KidLove.mber.vo.MberVO">
	
		SELECT 
			M.MBER_NO 
			, M.MBER_PUSH_TOKEN 
		FROM mber M 
		LEFT OUTER JOIN mber_topic MT 
		ON MT.MBER_NO  = M.MBER_NO 
		LEFT OUTER JOIN topic T 
		ON T.TOPIC_NO  = MT.TOPIC_NO 
		WHERE T.TOPIC_KEY =#{topic}
	
	</select>

</mapper>